# Relay 监听端口（默认 9223）
PORT=9223

# 鉴权 token（与 Chrome 扩展 options 中填写的值一致）
# 建议使用随机字符串，例如：openssl rand -hex 32
TOKEN=change-me-to-a-random-secret

# 允许的扩展 Origin（可填 * 在开发阶段，生产时改为扩展 ID）
# 示例：chrome-extension://abcdefghijklmnopqrstuvwxyz123456
ALLOW_ORIGINS=*
